import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  list: {
    paddingTop: '15px',
    marginRight: '40px',
  },
  playlist: {
    textAlign: 'center',
  },
  title: {
    paddingTop: '150px',
    paddingBottom: '20px',
  },
}));

export default useStyles;
